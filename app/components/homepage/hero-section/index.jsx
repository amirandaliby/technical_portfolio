import React from 'react';

const HeroSection = () => {
    return (
        <section id="hero">
            <h2>Hero Section</h2>
            <p>Content for Hero section.</p>
        </section>
    );
};

export default HeroSection;
